{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q '\\.ts\"'; then npx --no-install prettier --write \"$(echo \"$CLAUDE_TOOL_INPUT\" | sed -n 's/.*\"file_path\"\\s*:\\s*\"\\([^\"]*\\)\".*/\\1/p')\" 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
