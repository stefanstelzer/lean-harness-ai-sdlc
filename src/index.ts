/**
 * Public API of the lean-harness-ai-sdlc demo package.
 *
 * This barrel is the only entry point consumers should import from.
 */
export { FeatureFlags, hashToBucket } from './feature-flags.js';
export type { EvaluationContext, FeatureFlag } from './types.js';
