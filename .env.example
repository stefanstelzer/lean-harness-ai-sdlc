# Copy to .env and fill in. Never commit real secrets — the pre-push Trivy scan
# and PR pipeline will flag committed credentials.

# Example: anthropic API key used by the agent harness in your own projects.
ANTHROPIC_API_KEY=
